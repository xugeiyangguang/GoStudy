package unitTestExcercise

/*
import (
	"reflect"
	"testing"
)

func TestConstructor(t *testing.T) {
	type args struct {
		root *TreeNode
	}
	tests := []struct {
		name string
		args args
		want BSTIterator
	}{
		// TODO: Add test cases.
		{"1",args{creat173()},BSTIterator{record:[]int{5,2,3,1}}},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := Constructor(tt.args.root); !reflect.DeepEqual(got, tt.want) {
				t.Errorf("Constructor() = %v, want %v", got, tt.want)
			}
		})
	}
}

func creat173() *TreeNode {
	a2 := &TreeNode{2, nil, nil}
	a5 := &TreeNode{5, nil, a2}
	a1 := &TreeNode{1, nil, nil}
	a3 := &TreeNode{3, a5, a1}
	return a3
}*/
